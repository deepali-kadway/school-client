import { CapitalizationPipePipe } from './capitalization-pipe-pipe';

describe('CapitalizationPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizationPipePipe();
    expect(pipe).toBeTruthy();
  });
});
